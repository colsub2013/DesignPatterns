package patterns.behavioural.strategy;

/**
 *	1. Interfaz de estrategia que contiene declaracion de metodo 
 *	que sera implementado por clases concretas y se usa como
 *	referencia en el contexto 
 */
public interface StrategyIF {
	public void algorithmInterface(String parm);
}
